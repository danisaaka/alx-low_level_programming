#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

/**
 * add - add 2 numbers and return the result
 * @a: first operand
 * @b: second operand
 *
 * Return: Return the a + b.
 */
int add(int a, int b)
{
	return(a + b);
}
